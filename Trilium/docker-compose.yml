  trilium:
    image: ghcr.io/zadam/trilium:latest
    container_name: trilium
    restart: unless-stopped
    environment:
      - TRILIUM_DATA_DIR=/home/node/trilium-data
      - TRILIUM_TRUSTED_PROXIES=0.0.0.0/0
    volumes:
      - trilium-data:/home/node/trilium-data
    networks:
      - proxy
    expose:
      - "8080"
