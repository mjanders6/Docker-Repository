services:
  minecraft:
    image: mjanders6/cpu-temp-api:latest # cant be a local image
    # container_name: minecraft
    environment:
      - EULA=TRUE
      - PUID=1000
      - PGID=1000
    ports:
      - "25565:25565"
    user: "minecraft:minecraft"
    volumes:
      - server:/opt/minecraft
    deploy:
      placement:
        constraints:
          - "node.hostname == rpi0-server"  # Replace with your node's hostname
      resources:
        limits:
          memory: 3G
    # Comment out when using swarm
    # restart: unless-stopped
volumes:
  server:
    # When using swarm with docker compose. If not, comment out 
    external: true
