# Portainer on Swarm

Use the Docker Compose Portainer file and then follow [Install Portainer Agent on Docker Swarm](https://docs.portainer.io/admin/environments/add/swarm/agent) to create the Agents. 